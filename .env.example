# Zoho Mail SMTP Configuration
# Replace these values with your actual Zoho Mail credentials

# SMTP Server Settings
ZOHO_SMTP_HOST=smtp.zoho.com
ZOHO_SMTP_PORT=587

# Zoho Mail Credentials
# Use your Zoho email address
ZOHO_SMTP_USER=your-email@zoho.com
# Use an App Password (not your regular password)
# Generate one at: https://accounts.zoho.com/home#security/app-passwords
ZOHO_SMTP_PASS=your-app-password

# Email Addresses
# Email address to send from (usually same as ZOHO_SMTP_USER)
ZOHO_FROM_EMAIL=your-email@zoho.com
# Email address to receive form submissions
ZOHO_TO_EMAIL=reservations@lusoigardens.com
