name = "lusoi-gardens"
compatibility_date = "2025-11-25"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages automatically serves static assets from dist directory
# No need for 'site' configuration when using Cloudflare Pages

[build]
command = "npm run build"
cwd = "./"
